import React from 'react';

import { Box } from '@mui/material';

import Layout from '@/components/Layout';

const Home: React.FC = () => (
  <Layout>
    <Box>Main</Box>
  </Layout>
);

export default Home;
